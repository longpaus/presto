import { styled, Button } from '@mui/material';

const SubmitFormButton = styled(Button)({
  marginTop: '20px',
  color: 'white',
  backgroundColor: '#dd1461',
  width: '100%',
  '&:hover': {
    backgroundColor: '#dd1461',
  }
});

export default SubmitFormButton
